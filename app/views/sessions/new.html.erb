<%= form_for @user do |f|%>
<%= f.label :email %>
  <%= f.text_field :email %>
 

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "log in" %>
  or 
  
  <%= link_to('Log in with Facebook!', '/auth/facebook') %>
  <%end%>